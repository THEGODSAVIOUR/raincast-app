services:
  - type: web
    name: raincast-backend
    env: python
    rootDir: backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"

  - type: static_site
    name: raincast-frontend
    rootDir: frontend
    buildCommand: "npm install && npm run build"
    publishDirectory: build
    envVars:
      - key: REACT_APP_API_URL
        fromService:
          type: web
          name: raincast-backend
          property: url